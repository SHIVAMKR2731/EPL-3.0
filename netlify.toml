[build]
  publish = "frontend"
  command = "echo 'Static frontend ready'"

# Pass API requests directly to the deployed Render backend server
[[redirects]]
  from = "/api/*"
  to = "https://epl3-backend.onrender.com/api/:splat"
  status = 200
